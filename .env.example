# Copy to .env for local use, or set these in the Render dashboard.

# Shared secret required in the "Authorization: Bearer <token>" header.
# Leave unset locally to disable auth during development.
MCP_AUTH_TOKEN=change-me-to-a-long-random-string

# Port the server listens on. Render injects this automatically; default 8000.
PORT=8000
